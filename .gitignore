node_modules/
dist/
*.tgz
npm-debug.log*
.DS_Store
coverage/
.stryker-tmp/
reports/mutation/*.html
reports/mutation/*.json
packages/*/dist/
# Stray .vsix at the extension package root (the canonical copy lives in releases/)
packages/vscode-spec-driven/*.vsix
_site/
# Local-only evaluation, never committed
mejoras/aetherdeploy-assessment.md
scripts/demo/**/out/
.smoke-tmp/
# JVM plugin build output (Java sources are tracked, build artefacts are not)
# Wildcards, not one line per package: `intellij-spec-driven` had 17 of these
# committed because C0-01 only ever named `gradle-plugin`, and every session
# since carried four modified lock files in its working tree.
packages/*/.gradle/
packages/*/build/
packages/maven-plugin/target/
# IDE
.idea/
sbom.cyclonedx.json
# Book project — local only, never committed
BOOK_PLAN.md
GEMINI_BOOK_PROMPT.md
book/
